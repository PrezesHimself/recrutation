'use strict';

class PhotosController {

    constructor($scope) {
        console.log('asdf');

    }

    nextSlide($event) {
        console.log($event);
    }

    prevSlide($event) {
        console.log($event);
    }
}

export default PhotosController;

